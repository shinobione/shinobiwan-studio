import assert from 'node:assert/strict';
import fs from 'node:fs';

const albumApi = fs.readFileSync('src/services/album-admin-api.ts', 'utf8');
const albumUi = fs.readFileSync('src/components/AlbumManager.tsx', 'utf8');
const app = fs.readFileSync('src/App.tsx', 'utf8');
const router = fs.readFileSync('src/router.ts', 'utf8');
const types = fs.readFileSync('src/types/studio.ts', 'utf8');
const main = fs.readFileSync('src/main.tsx', 'utf8');
const release = fs.readFileSync('src/release.ts', 'utf8');
const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
for (const required of ["create: 'album-create-v1'","metadata: 'album-metadata-save-v1'","membership: 'album-membership-save-v1'","move: 'album-track-move-v1'","upload: 'album-asset-upload-v1'","deleteAsset: 'album-asset-delete-v1'","'/api/studio/albums'","credentials: 'include'","'Content-Type': 'text/plain;charset=UTF-8'","requireManage('album-create')","requireManage('album-metadata')","requireManage('album-membership')","requireManage('album-move')","requireManage('album-assets')",'getAdminAlbum(albumId)','return verify(albumId, payload)']) assert.ok(albumApi.includes(required), `C2.5-D Album client missing: ${required}`);
for (const forbidden of ['MEDIA_BUCKET','wrangler','api/media/albums','deleteStudioAlbum(']) assert.ok(!albumApi.includes(forbidden), `Studio Album client must not bypass Track Manager or expose whole-Album deletion: ${forbidden}`);
for (const required of ['Canonical releases, finally managed in Studio.','+ New Album / EP','Create canonical draft','album.trackIds','Save tracklist','Move to…','Upload cover + thumbnail','Apply palette to metadata form','C2.5-D boundary','C2.5-E/F','Legacy assignment locked until C2.5-E']) assert.ok(albumUi.includes(required), `Album Manager UI missing: ${required}`);
assert.ok(!albumUi.includes('Add an unowned track…'), 'D1 must not turn manual legacy-track assignment into an accidental C2.5-E migration.');
assert.ok(!albumUi.includes('Delete Album'), 'C2.5-D must not expose whole-Album deletion.');
assert.ok(albumUi.includes('globalThis.confirm'), 'Production Album mutations must retain explicit confirmation.');
assert.ok(app.includes("route: 'albums'"), 'Studio navigation must expose Albums / Projects.');
assert.ok(app.includes("route === 'albums' && <AlbumManager />"), 'Albums route must mount Album Manager.');
assert.ok(app.includes('Track Manager v5.17 · bridge v1.9'), 'Studio shell must report deployed C2.5-C backend.');
assert.ok(app.includes('<strong>6.3.8</strong>'), 'Studio shell must keep current LRC Maker version.');
assert.ok(router.includes("'albums'"), 'Router must recognize Albums.');
assert.ok(types.includes("| 'albums'"), 'StudioRoute must include Albums.');
assert.ok(main.includes("import './album-management.css';"), 'Album styles must be loaded.');
assert.ok(main.includes("import './c2-5-d-navigation.css';"), 'Four-destination mobile navigation override must be loaded.');
assert.ok(release.includes("version: '0.11.0'") && release.includes('build: 32'), 'C2.5-D1 release must be v0.11.0 Build 32.');
assert.equal(pkg.version, '0.11.0');
assert.ok(String(pkg.scripts?.['check:ux'] || '').includes('test-phase-ux-c2-5-d-albums.mjs'));
console.log('Studio 0.11.0 Build 32 exposes guarded canonical Album Management through Track Manager v5.17 without direct R2 access, legacy migration, whole-Album deletion or Phase 7 work.');
