# SHINOBIWAN STUDIO — Canonical Roadmap

Updated: 2026-09-13 after **Build107 REAL USER PASS** and Phase10 Slice1 acceptance closeout.

This file tracks durable Done / Active / Next / Backlog state. Historical implementation detail belongs in `changelogs/`, `docs/` and acceptance receipts.

## Done

### Foundation / integration

- Phases 0–6 — complete.
- Phase 7-A — complete / REAL USER PASS.
- Phase 7-B — complete / REAL USER PASS.
- Phase 7-C — complete / program closeout.

Accepted workflow authority remains:

```text
Identity → Core media → Lyrics → Intelligence → Release
```

### Phase 8 — Content Health / semantic truth

Accepted through Build81. Content Health, Album Health, publication truth and Sonic/provider semantic cleanup are closed and must not be reopened merely for refactoring.

### Phase 9 — reliability / canonical truth — PROGRAM COMPLETE

Phase9 is closed on accepted **Studio v0.19.28 · Build106**. Its reliability contracts remain frozen and Build107 does not alter them.

Accepted Build106 receipt: [`docs/acceptance/BUILD106-REAL-USER-PASS.md`](docs/acceptance/BUILD106-REAL-USER-PASS.md).

Phase9 closeout audit: [`docs/PHASE-9-PROGRAM-CLOSEOUT-AUDIT.md`](docs/PHASE-9-PROGRAM-CLOSEOUT-AUDIT.md).

### Phase 10 Slice1 — Build107 shared catalog projection kernel — REAL USER PASS

Build107 is the first accepted Phase10 progressive-extraction slice.

The cross-repository audit proved one exact duplicated numerical responsibility shared by Studio and SonicTrace. Build107 extracted only:

```text
vector normalization
dot product
powerComponent
```

SonicTrace now owns the sole editable module at `js/catalog-projection-kernel.mjs`. Studio consumes a generated byte-pinned offline copy with committed source provenance and SHA-256 verification.

Preserved outside the kernel:

- Studio 512D validation, ordering, centering, map normalization, zones, nearest-track behavior and semantic policy;
- SonicTrace input cleaning, projection policy, fallback, clustering, scoring and semantic policy;
- standalone operation of both applications;
- Track Manager, R2, LaunchPAD, LRC Maker and release-orchestration authority.

Implementation PRs and proof:

```text
SonicTrace PR          #86
SonicTrace source      70b0bf277a01f3817dd2db71c91233a8fe91debf
SonicTrace CI          #107 · 34749669665 · SUCCESS
Studio PR              #214
Studio implementation  b827e7076610c840f8e0f4446f5cf6fd33411008
Studio CI              #617 · 34749718962 · SUCCESS
Studio merge           9b40d2cc3fb1bd22e950a94af859564fadf8dfff
Studio Pages           #225 · 34750194798 · SUCCESS build + deploy
Real-user smoke        PASS · Studio + SonicTrace catalog maps
```

Accepted Build107 receipt: [`docs/acceptance/BUILD107-REAL-USER-PASS.md`](docs/acceptance/BUILD107-REAL-USER-PASS.md).

## Active

### Phase 10 — progressive extraction

Phase10 is active as a **program**, not as permission for continuous refactoring.

There is currently no allocated Slice2 build. Build107 is complete and accepted.

## Next

### Fresh bounded Phase10 Slice2 scope audit

Before implementation, perform a new read-only cross-repository audit. The next slice must independently prove all of the following:

```text
1. a real duplicated or reusable mature capability exists;
2. one canonical editable authority can remain singular;
3. extraction reduces duplication instead of creating a second source of truth;
4. standalone Studio / SonicTrace / LRC Maker / LaunchPAD behavior stays safe;
5. Studio remains orchestration/UI rather than becoming a monolith;
6. rollback is independently bounded;
7. CI and real-user acceptance boundaries are explicit.
```

Do not assume LRC, SonicTrace profiles, catalog contracts or any broader engine should be extracted merely because Phase10 exists.

## Backlog

### Reliability candidates requiring stronger backend contracts

These remain beyond Phase9 and outside Build107:

- Album create lost-response causality / operation identity;
- Track create lost-response causality / operation identity;
- exact-byte/digest proof for binary upload families;
- catalog rebuild operation identity/generation evidence;
- Deep Audio request status/idempotency if the coordinator later gains an operation identity contract;
- degraded/offline behavior that materially affects the private Studio workflow.

### Premium interaction polish

Rolling, non-blocking product polish remains preserved:

- tactile press/release feedback;
- restrained glow/focus transitions;
- coherent hover/active states;
- smooth panel/tab transitions;
- reduced-motion-safe animation;
- no decorative motion that obscures state or slows work.

This polish may be folded into a future bounded product slice only when it does not blur the scope or acceptance boundary.

### Future Phase10 extraction candidates

Candidates remain hypotheses until audited:

- mature LRC synchronization boundaries;
- SonicTrace analysis/profile/catalog logic not already correctly reused;
- additional catalog logic only where exact duplication is proven;
- shared contracts/types only when authority remains singular and standalone apps remain safe.

There is currently **no official Phase 11**.

## Frozen roadmap constraints

- Do not create a second queue, workflow-priority engine, Album authority or generic write service.
- Do not reopen completed phases merely because historical docs are old or verbose.
- Do not use a new build as a bucket for opportunistic refactors.
- Do not treat a deployed candidate as accepted until real-user validation exists where required.
- Do not deliberately damage or interrupt production merely to prove retry/ambiguity behavior.
- Do not generalize GET retry into write retry.
- Do not generalize non-mutating validation retry into write retry.
- Do not fake causal proof when the backend exposes no operation identity/digest/status evidence.
- Build101 and Build104 remain rejected historical evidence.
- Phase9 is complete.
- Build107 is accepted Phase10 Slice1 and must not expand retroactively.
- Any Phase10 Slice2 requires a fresh bounded audit.

## Current acceptance pointer

See `PROJECT_STATE.md` for current runtime/cross-stack truth, `QA.md` for accepted validation boundaries, and [`docs/acceptance/BUILD107-REAL-USER-PASS.md`](docs/acceptance/BUILD107-REAL-USER-PASS.md) for the latest accepted Studio runtime receipt.
